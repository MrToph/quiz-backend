// When you clone the repository make sure to change and hide these from public, otherwise your JWT authentication is insecure
// as anyone can sign the header + payload
export const signingKey = 'ThisIsJustATest'
